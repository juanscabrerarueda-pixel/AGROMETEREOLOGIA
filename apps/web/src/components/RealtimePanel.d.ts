import type { Series } from '@pkg/core';
type RealtimePanelProps = {
    series?: Series | null;
    busy?: boolean;
};
export declare function RealtimePanel({ series, busy }: RealtimePanelProps): import("react/jsx-runtime").JSX.Element;
export {};
